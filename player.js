const U="https://ifufmrgoknrqwbtxydmr.supabase.co",K="sb_publishable_KXTCeSGR9LIpvqr15TOXBw_fIK9X1Rx",db=supabase.createClient(U,K),$=id=>document.getElementById(id),esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));(async()=>{const id=Number(new URLSearchParams(location.search).get("id"));const p=await db.from("players").select("*").eq("id",id).maybeSingle();if(p.error||!p.data){$("playerPage").innerHTML='<div class="panel">Player not found.</div>';return}const c=await db.from("completions").select("*,levels(name,rating)").eq("player_id",id).eq("status","approved").order("created_at",{ascending:false});const rows=c.data||[];$("playerPage").innerHTML=`<section class="panel"><p class="eyebrow">PLAYER PROFILE</p><h1>${esc(p.data.username)}</h1><div class="profile-card"><div class="stat"><span class="muted">Verified completions</span><b>${rows.length}</b></div><div class="stat"><span class="muted">Hardest current rank</span><b>${rows.length?Math.min(...rows.map(x=>x.levels?.rating||999999)).toFixed(0):"—"}</b></div></div><h2>Verified Completions</h2>${rows.length?rows.map(x=>`<div class="level-row"><div class="level-main"><h3>${esc(x.levels?.name||x.level_id)}</h3><div class="level-meta">Rating ${Math.round(x.levels?.rating||0)} · ${new Date(x.created_at).toLocaleDateString()}</div></div><a href="${esc(x.youtube_url)}" target="_blank" rel="noopener">Proof ↗</a></div>`).join(""):'<div class="empty">No verified completions.</div>'}</section>`})();