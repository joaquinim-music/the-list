const SUPABASE_URL="https://ifufmrgoknrqwbtxydmr.supabase.co",SUPABASE_KEY="sb_publishable_KXTCeSGR9LIpvqr15TOXBw_fIK9X1Rx",db=supabase.createClient(SUPABASE_URL,SUPABASE_KEY),$=id=>document.getElementById(id),esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
async function main(){const id=Number(new URLSearchParams(location.search).get('id'));if(!id){$('levelPage').innerHTML='<div class="panel">Missing level ID.</div>';return}const lq=await db.from('levels').select('*').eq('id',id).maybeSingle();if(lq.error||!lq.data){$('levelPage').innerHTML='<div class="panel">Level not found.</div>';return}let l=lq.data,api={};try{const r=await fetch(`https://gdbrowser.com/api/level/${id}`);if(r.ok)api=await r.json()}catch{}const ranked=await db.from('levels').select('id').order('rating',{ascending:false});const rank=(ranked.data||[]).findIndex(x=>Number(x.id)===id)+1;const v=await db.from('completions').select('player_id,players(username),youtube_url,created_at').eq('level_id',id).eq('status','approved').order('created_at',{ascending:true});const hist=await db.from('rating_history').select('rating,comparisons,created_at').eq('level_id',id).order('created_at',{ascending:true});$('levelPage').innerHTML=`<section class="panel"><p class="eyebrow">LEVEL #${id}</p><div class="level-title-row"><div><h1>${esc(api.name||l.name)}</h1><p class="muted">by ${esc(api.author||l.creator||'Unknown creator')}</p></div><div class="rank-badge">#${rank||'—'}<span>The List</span></div></div><div class="stat-grid"><div class="stat-card"><strong>${Math.round(l.rating)}</strong><span>List rating</span></div><div class="stat-card"><strong>${l.comparisons||0}</strong><span>Comparisons</span></div><div class="stat-card"><strong>${esc(api.difficulty||l.gd_difficulty||'—')}</strong><span>GD difficulty</span></div><div class="stat-card"><strong>${v.data?.length||0}</strong><span>Verified victors</span></div></div><div class="level-facts"><span><b>Song:</b> ${esc(api.songName||l.song_name||'Unknown')}</span><span><b>Song artist:</b> ${esc(api.songAuthor||'Unknown')}</span><span><b>Downloads:</b> ${api.downloads??'—'}</span><span><b>Likes:</b> ${api.likes??'—'}</span></div><h2>Description</h2><p class="description">${esc(api.description||l.description||'No description available.')}</p><h2>Rating History</h2><div id="history-inline">${renderHistory(hist.data||[])}</div><h2>Verified Victors</h2>${v.data?.length?v.data.map(x=>`<div class="victor"><a href="player.html?id=${x.player_id}">${esc(x.players?.username||'Player')}</a><a href="${esc(x.youtube_url)}" target="_blank" rel="noopener">Watch proof ↗</a></div>`).join(''):'<div class="empty">No verified victors yet.</div>'}</section>`}
function renderHistory(rows){if(!rows.length)return '<div class="empty">No rating history yet. History begins with the next comparison.</div>';const vals=rows.map(x=>Number(x.rating));const min=Math.min(...vals),max=Math.max(...vals),range=Math.max(1,max-min),w=760,h=230,p=28;const pts=vals.map((v,i)=>{const x=p+(i/(Math.max(1,vals.length-1)))*(w-p*2);const y=h-p-((v-min)/range)*(h-p*2);return [x,y]});const poly=pts.map(p=>p.join(',')).join(' ');return `<div class="history-chart"><svg viewBox="0 0 ${w} ${h}" role="img" aria-label="Rating history"><line x1="${p}" y1="${h-p}" x2="${w-p}" y2="${h-p}" stroke="currentColor" opacity=".2"/><polyline points="${poly}" fill="none" stroke="currentColor" stroke-width="4" stroke-linejoin="round" stroke-linecap="round"/>${pts.slice(-1).map(q=>`<circle cx="${q[0]}" cy="${q[1]}" r="5" fill="currentColor"/>`).join('')}</svg><div class="history-meta"><span>${Math.round(min)} min</span><b>${Math.round(vals.at(-1))} current</b><span>${Math.round(max)} max</span></div></div>`}
main();